PORT=3001
SESSION_SECRET=your-secret-key-change-this

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Seed database with example configurations on startup (set to 'true' to enable)
SEED_DB=true

# Note: OAuth/OIDC/SAML configurations are now stored in the database
# Use the API endpoint POST /api/clients to configure authentication providers
